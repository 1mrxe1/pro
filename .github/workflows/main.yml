name: Python Bot

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"

permissions:
  contents: write

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi

      - name: Run bot
        run: python main.py

      - name: Commit & Push files in same folder as main.py
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          MAIN_DIR=$(dirname "$(find . -name main.py | head -n 1)")
          find "$MAIN_DIR" -maxdepth 1 -type f -exec git add {} \;
          git commit -m "Auto-update files near main.py" || echo "No changes"
          git push
